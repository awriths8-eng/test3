name: "CodeQL Advanced"
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '19 13 * * 6'
jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
        - language: c-cpp
          build-mode: manual
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v4
      with:
        languages: ${{ matrix.language }}
        build-mode: ${{ matrix.build-mode }}
        queries: security-extended, security-and-quality, security-experimental

    # 手動編譯步驟：因為專案沒有 Makefile / CMake，直接用 gcc 編譯
    - name: Build
      run: |
        gcc -c main.c -o main.o

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v4
      with:
        category: "/language:${{matrix.language}}"

    # 方式 A（最推薦）：使用 GitHub 官方提供的特化 Action 匯出 SARIF
    - name: Upload SARIF Artifact
      uses: actions/upload-artifact@v4
      with:
        name: codeql-sarif-report
        # 使用 runner.temp 取得絕對路徑，避開相對路徑 '..' 限制
        path: ${{ runner.temp }}/codeql_databases/c-cpp/results/*.sarif  
